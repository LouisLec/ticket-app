query GetAllProjectsByOrganizationId($organizationId: UUID!) {
  projects(
    orderBy: [ORDER_ASC]
    condition: { organizationId: $organizationId }
  ) {
    nodes {
      ...ProjectFragment
    }
  }
}
